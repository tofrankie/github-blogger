## 1. 重整入口命名

- [x] 1.1 将 `extension/panels/multi-select-input.ts` 重命名为更贴近职责的文件名，并同步更新导入引用
- [x] 1.2 更新 `extension/index.ts` 中的命令注册调用，确保打开和配置仍然走同一个初始化入口
- [x] 1.3 同步整理入口文件内部对应的方法、变量和类型命名，保证新旧职责边界一致

## 2. 拆分初始化职责

- [x] 2.1 把通用的多步输入流程抽成独立模块，保留返回、取消和校验能力
- [x] 2.2 将配置写入、缓存失效、仓库创建和成功/失败提示整理为清晰的编排层
- [x] 2.3 保持现有设置键和值不变，确认已有仓库与新仓库的结果分支行为一致
- [x] 2.4 把步骤顺序抽成可直接调整的数组或等价声明，保证只改顺序时不需要改执行逻辑

## 3. 补充文档与验证

- [x] 3.1 将 `docs/draft/20260722-2.md` 整理成可交付的初始化流程说明文档
- [x] 3.2 运行类型检查或等效验证，确认重构后的命名和导入路径可以正常编译通过
